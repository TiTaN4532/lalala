{% extends "LaNetLaNetBundle::layout.html.twig" %}

{% block slider %}
{% endblock %}
{% block content %}
<h1>{{ brand.name }}</h1>
             
  <div class="row">
    <div class="col col_1_of_2"><img src="{{ asset(brand.getWebPath)}}" style="width:100%;" /></div>
    <div class="col col_1_of_2">
      <p>{{ brand.country }}</p>
      <p>{{ brand.link }}</p>
      <p>{{ brand.description }}</p>
    </div>
  </div>
{% endblock %}