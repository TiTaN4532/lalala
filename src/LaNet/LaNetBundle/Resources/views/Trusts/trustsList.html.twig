{% extends "LaNetLaNetBundle::layout.html.twig" %}

{% block slider %}
{% endblock %}
{% block content %}
 <h1 style="float:left;margin-right: 10px;">Им доверяют</h1><hr align="right" style="width:auto;margin: 37px; padding: 0 10px; " size="2" color="#AAA" />
              {% for post in trusts %}
              {% if not loop.first %} <hr /> {% endif %}
               <div class="row">
                    <div class="col col_1_of_3" style='margin-bottom: 20px;'><img src="{{ post.getWebPath | imagine_filter('my_news_big')}}" style="width:100%;" /></div>
                    <div class="col col_2_of_3" style="font-weight: 900">{{ post.title }}</div>
                    <div class="col col_2_of_3">{{ post.description | raw }} <a href="{{ path('la_net_la_net_trusts_id', { 'slug' : post.slug } ) }}" class="blue-link">Подробнее</a> </div>
                    {#<div class="col col_1_of_2" style="color:#2d93b2;">
                    Добавлена на сайт {{ post.updated | localizeddate('long', 'short')}}
                </div>#}
                {#<div class="col col_1_of_2" style="color:#2d93b2;">
                    Отправить:
                </div>#}
              </div>
              {% else %}
                Пока статей нет
              {% endfor %}
{% endblock %}